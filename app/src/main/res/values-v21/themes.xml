<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/violet_dark</item>
        <item name="colorPrimaryDark">@color/violet_dark</item>
        <item name="colorAccent">@color/violet_lighter</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">true</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:colorControlHighlight">@color/violet_lighter</item>

        <!-- Color of the drawer arrow -->
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
        <item name="colorControlNormal">@color/colorItem</item>

        <!-- FragmentSettings-->
        <!--<item name="preferenceTheme">@style/PreferenceThemeOverlay</item>-->
        <item name="preferenceTheme">@style/PreferenceThemeOverlay.v14.Material</item>

        <!-- Popup menu -->
        <item name="android:itemBackground">@color/colorBackground</item>
        <item name="android:popupMenuStyle">@style/PopupMenu</item>

        <!-- Enable transitions -->
        <item name="android:windowContentTransitions">true</item>

        <item name="android:windowEnterTransition">@transition/body_transform</item>
        <!--<item name="android:windowExitTransition">@transition/body_transform</item>-->
        <!--&lt;!&ndash; specify shared element transition &ndash;&gt;-->
        <item name="android:windowSharedElementEnterTransition">
        @transition/change_title_transform</item>
        <!--<item name="android:windowSharedElementExitTransition">-->
        <!--@transition/body_transform</item>-->
    </style>
</resources>